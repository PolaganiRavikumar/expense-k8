apiVersion: v1
kind: ConfigMap
metadata:
   name: backend
   namespace: expense
data:
  DB_HOST: mysql

apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: expense
  lables:
    app: backend
    tier: api
    project: expense
spec:
  replicas: 2
  selector:
  matachlabels:
    app: backend
    tier: api
    project: expense
template:
  metadata:
    labels:
      app: backend
      tier: api
      project: expense
spec:
  containers:
     - name: backend
       image: ravi307/backend:v1
       envFrom:
         -ConfigMap Ref:
              name: backend
              